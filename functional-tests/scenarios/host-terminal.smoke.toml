schema = 1
id = "host-terminal.smoke"
behavior_ids = ["BHV-CMD-LOCAL", "BHV-HOST-TERMINAL", "BHV-LIFECYCLE-STARTED", "BHV-ACTION-FOCUS", "BHV-ACTION-TYPE-TEXT", "BHV-ACTION-KEY", "BHV-ACTION-FINISH", "BHV-LIFECYCLE-EXITED", "BHV-LIFECYCLE-CLEANUP"]
surface = "host_terminal"
capabilities = ["real_host_terminal", "real_os_keyboard"]
fixture = "host_terminal_probe"
estimated_cost_ms = 5000
deadlines = { action_ms = 15000, startup_ms = 30000, cleanup_ms = 10000, scenario_ms = 60000 }
required_evidence = ["event_log", "stdout", "stderr", "process_tree", "screenshot_on_failure"]
actions = [{ type = "focus_window" }, { type = "type_text", text = "host-terminal-input-ok" }, { type = "key", key = "Enter" }, { type = "finish" }]
checkpoints = [{ type = "exit_status", code = 0 }, { type = "resources_zero" }]
