schema = 1
id = "startup.bench"
behavior_ids = ["BHV-CMD-BENCH", "BHV-ACTION-FINISH", "BHV-LIFECYCLE-EXITED", "BHV-LIFECYCLE-CLEANUP"]
surface = "console"
fixture = "benchmark"
estimated_cost_ms = 1500
required_evidence = ["event_log", "stdout", "stderr", "process_tree"]
actions = [{ type = "finish" }]
checkpoints = [{ type = "terminal_contains", text = "throughput_bytes_per_sec" }, { type = "exit_status", code = 0 }, { type = "resources_zero" }]
