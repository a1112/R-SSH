{
  "$schema": "../../gen/schemas/desktop-schema.json",
  "identifier": "functional-observer-capability",
  "description": "Functional-only read-only state publication from the loopback Web UI.",
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "windows": ["main"],
  "permissions": ["allow-functional-observer-publish"]
}
