{
  "name": "rssh-tauri",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:functional": "tauri build --config src-tauri/tauri.functional.conf.json --features functional-test-observer",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
